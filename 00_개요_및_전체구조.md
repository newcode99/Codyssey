# 본과제 1 — Chapter 00: 과제 전체 개요 및 구조
> 지식 0%인 사람이 이 문서를 읽고 "이 과제가 무엇인지, 왜 하는지, 무엇을 만드는지"를 완벽히 이해할 수 있어야 함.

---

## 00-1. 이 과제를 한 문장으로

> **"실제 서버를 운영하는 엔지니어처럼, Linux 서버에 보안을 설정하고 서버 상태를 매분 자동으로 기록하는 감시 시스템을 처음부터 끝까지 직접 구축한다."**

---

## 00-2. 왜 이 과제가 존재하는가? (현실 문제)

```
[로그 없는 서버에서 장애가 발생하면]

새벽 3시. 서비스가 갑자기 먹통이 됨.
→ 개발자 긴급 출근
→ "왜 죽었지?" — 아무 단서 없음
→ CPU 폭증? 메모리 부족? 프로세스 충돌?
→ 감으로 6시간 추측하며 복구
→ 다음 달 같은 장애 반복

[이 과제를 마친 후 동일한 상황에서]

새벽 3시. 서비스 다운.
→ 로그 파일 열기
→ [2026-05-14 02:58:01] CPU:95% MEM:87% DISK_USED:99%
→ "02:58에 CPU·메모리·디스크가 동시에 폭발했네"
→ 원인 파악 30분 → 복구 완료

핵심 교훈:
"로그는 장애가 발생하기 전부터 쌓여야 한다."
"사람이 없어도 서버가 스스로 상태를 기록해야 한다."
```

---

## 00-3. 이 과제의 전체 구조 (한눈에 보기)

```
┌─────────────────────────────────────────────────────┐
│                  본과제 1 전체 흐름                   │
├──────────────┬──────────────┬───────────────────────┤
│  ① 보안 기반  │  ② 권한 체계  │  ③ 앱 실행 환경        │
│              │              │                       │
│ SSH 포트변경  │  계정 3개     │  환경 변수 설정         │
│ Root 차단    │  그룹 2개     │  키 파일 생성           │
│ 방화벽 설정   │  ACL 권한    │  앱 Boot Sequence 실행 │
└──────┬───────┴──────┬───────┴────────────┬──────────┘
       │              │                    │
       └──────────────┴────────────────────┘
                          │
                          ▼
       ┌──────────────────────────────────────┐
       │      ④ monitor.sh (핵심 결과물)       │
       │                                      │
       │  프로세스 확인 → 포트 확인             │
       │  → CPU/MEM/DISK 수집                 │
       │  → 임계값 경고 출력                   │
       │  → 로그 파일에 기록                   │
       │  → 로그 용량 관리 (10MB/10개)         │
       └──────────────────┬───────────────────┘
                          │
                          ▼
       ┌──────────────────────────────────────┐
       │       ⑤ crontab 자동 실행             │
       │                                      │
       │  매분 monitor.sh 자동 실행             │
       │  /var/log/agent-app/monitor.log      │
       │  에 1분 간격으로 줄 누적               │
       └──────────────────────────────────────┘
```

---

## 00-4. 완성 후 서버의 모습

```
[완성된 시스템이 하는 일]

1. 보안:
   - 22번 포트 닫힘 → 해커 자동 공격 무력화
   - 20022번으로만 SSH 접속 가능
   - Root 계정 원격 접속 불가
   - 방화벽: 20022, 15034번만 열려있음

2. 권한 분리:
   - agent-admin → 서버 관리 + 자동 실행
   - agent-dev   → 스크립트 개발 + 로그 접근
   - agent-test  → 공용 파일만 접근
   - api_keys    → admin, dev만 접근 (test 차단)

3. 앱 실행:
   - python3 agent_app.py → 5단계 자가 점검
   - "Agent READY" 출력 후 15034번 포트 대기

4. 자동 모니터링:
   - 매분 monitor.sh 실행
   - 앱 살아있는지 → 포트 열려있는지 → CPU/MEM/DISK 수집
   - 로그 파일에 기록:
     [2026-05-14 16:00:01] PID:1234 CPU:5% MEM:12% DISK_USED:23%
     [2026-05-14 16:01:01] PID:1234 CPU:7% MEM:12% DISK_USED:23%
     [2026-05-14 16:02:01] PID:1234 CPU:6% MEM:13% DISK_USED:23%
```

---

## 00-5. 제출해야 하는 것 (완전 목록)

```
[제출물 1: 수행 내역서 (문서)]
형식: Word, PDF, Markdown 중 하나

포함해야 할 증거 스크린샷 8가지:
① SSH 포트(20022) + PermitRootLogin no 확인
② UFW 방화벽 20022/tcp, 15034/tcp 허용 확인
③ agent-admin, dev, test 계정 + agent-common, core 그룹 확인
④ 디렉토리 구조 + ACL 권한 확인
⑤ Boot Sequence 5단계 [OK] + "Agent READY" 확인
⑥ monitor.sh 실행 전체 출력 확인
⑦ /var/log/agent-app/monitor.log 최근 기록 확인
⑧ crontab 등록 + 1~2분 후 로그 자동 증가 확인

[제출물 2: monitor.sh 소스코드]
파일 하나 (shell script, .sh 확장자)
→ 전체 코드는 04_구현_STEP7~10.md 참조
```

---

## 00-6. 10단계 진행 순서 (전체 로드맵)

| STEP | 작업 | 파일 참조 |
|------|------|---------|
| 1 | Ubuntu 환경 준비 (Docker) | 02_구현_STEP1~3.md |
| 2 | SSH 포트 20022 변경 | 02_구현_STEP1~3.md |
| 3 | Root 접속 차단 | 02_구현_STEP1~3.md |
| 4 | UFW 방화벽 설정 | 02_구현_STEP1~3.md |
| 5 | 계정 3개 + 그룹 2개 생성 | 03_구현_STEP4~6.md |
| 6 | 디렉토리 구조 + ACL 권한 설정 | 03_구현_STEP4~6.md |
| 7 | 환경 변수 + 키파일 설정 | 03_구현_STEP4~6.md |
| 8 | agent-app 실행 + Boot Sequence 확인 | 04_구현_STEP7~10.md |
| 9 | monitor.sh 작성 + 권한 750 설정 | 04_구현_STEP7~10.md |
| 10 | crontab 매분 등록 + 자동 실행 확인 | 04_구현_STEP7~10.md |

---

## 00-7. 이 과제가 현업에서 의미하는 것

| 과제에서 배우는 것 | 현업에서 동일한 개념 |
|-----------------|-------------------|
| SSH 보안 설정 | 클라우드 서버 초기 보안 설정 |
| UFW 방화벽 | AWS Security Group, GCP Firewall Rules |
| 계정/그룹/ACL | AWS IAM Role, Kubernetes RBAC |
| 환경 변수 | 12-Factor App, Docker ENV, k8s ConfigMap |
| Shell Script | DevOps 자동화, CI/CD 파이프라인 스크립트 |
| crontab | AWS CloudWatch Events, k8s CronJob |
| 로그 관리 | ELK Stack, Datadog, Splunk의 근본 원리 |

---

## 00-8. 이 가이드를 읽는 순서

```
1. 00_개요_및_전체구조.md     ← 지금 읽는 파일 (과제 이해)
2. 01_핵심개념_완전이해.md    ← Linux/권한/Shell 개념
3. 02_구현_STEP1~3.md        ← 환경 준비 + 보안 설정
4. 03_구현_STEP4~6.md        ← 계정/권한/환경변수
5. 04_구현_STEP7~10.md       ← 앱실행/monitor.sh/crontab
6. 05_검증_및_오류해결.md     ← 최종 체크 + 트러블슈팅
```
