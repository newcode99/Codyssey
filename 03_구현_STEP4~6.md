# 본과제 1 — Chapter 03: 구현 STEP 4~6
## 계정/그룹 관리 + 권한(ACL) 제어 + 환경 변수

> ⚠️ **시작 전 필수 확인**
> 이 단계의 작업은 모두 **root 계정**(최고 관리자)에서 진행함.
> (STEP 6의 환경 변수 등록 시에만 agent-admin으로 전환)

---

## STEP 4: 계정 및 그룹 생성

### 4-1. 계정 3개 생성

```bash
# 계정 생성을 위한 명령어 (루트 계정에서 실행)
# 옵션 복습:
# -m = 홈 디렉토리(/home/계정명) 생성
# -s /bin/bash = 기본 쉘을 bash로 설정

useradd -m -s /bin/bash agent-admin
useradd -m -s /bin/bash agent-dev
useradd -m -s /bin/bash agent-test

# ─────────────────────────────────────────
# [꼬리물기] 만약 -m 옵션을 빼먹었다면?
# ─────────────────────────────────────────
# 증상: 계정은 생성되지만 /home/계정명 폴더가 없음
# 로그인 시 "No directory, logging in with HOME=/" 에러 발생
# 
# 해결법 (이미 만들었을 경우):
# 1. 수동으로 폴더 생성: mkdir /home/agent-admin
# 2. 소유자 변경: chown agent-admin:agent-admin /home/agent-admin
```

### 4-2. 비밀번호 설정

```bash
# 각 계정마다 비밀번호를 설정해야 로그인이 가능해짐
passwd agent-admin
# New password: (비밀번호 입력 - 화면에 표시되지 않음)
# Retype new password: (다시 입력)

passwd agent-dev
# 비밀번호 설정

passwd agent-test
# 비밀번호 설정
```

### 4-3. 그룹 2개 생성

```bash
# 그룹 생성
groupadd agent-common
groupadd agent-core

# ─────────────────────────────────────────
# [꼬리물기] 그룹은 왜 필요한가요?
# ─────────────────────────────────────────
# 과제 요구사항:
# upload_files 폴더는 admin, dev, test 세 명 모두 사용 (공용)
# api_keys 폴더는 admin, dev 두 명만 사용 (보안)
#
# 폴더 권한을 줄 때 "A, B, C에게 권한 줘"라고 일일이 설정하는 것보다
# "A, B, C를 'common' 그룹에 넣고, 'common' 그룹에 권한 줘"
# 라고 설정하는 것이 관리하기 훨씬 쉽기 때문임.
```

### 4-4. 계정을 그룹에 할당 (매우 중요!)

```bash
# usermod = User Modify (계정 수정)
# -a = append (추가)
# -G = Group (그룹)
# ⚠️ 주의: -a 없이 -G만 쓰면, 계정이 원래 속해있던 그룹에서 쫓겨나고 새 그룹에만 속하게 됨!

# 1) agent-common 그룹 (공용) - 3명 모두 추가
usermod -aG agent-common agent-admin
usermod -aG agent-common agent-dev
usermod -aG agent-common agent-test

# 2) agent-core 그룹 (보안) - admin, dev만 추가 (test는 제외!)
usermod -aG agent-core agent-admin
usermod -aG agent-core agent-dev

# 3) 할당 결과 확인
id agent-admin
# uid=1001(agent-admin) gid=1001(agent-admin) groups=1001(agent-admin),1004(agent-common),1005(agent-core)
# → groups 항목에 agent-common, agent-core 두 개 모두 보여야 함

id agent-dev
# → groups 항목에 agent-common, agent-core 두 개 모두 보여야 함

id agent-test
# → groups 항목에 agent-common만 보이고, agent-core는 없어야 함!

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ③
# id agent-admin
# id agent-dev
# id agent-test
# 위 세 명령어의 실행 결과를 모두 한 화면에 보이게 캡처
# ────────────────────────────────
```

---

## STEP 5: 디렉토리 구조 및 세밀한 권한(ACL) 설정

### 5-1. 기초 디렉토리 구조 생성

```bash
# AGENT_HOME 경로 변수 임시 지정 (타이핑 줄이기 위함)
AGENT_HOME="/home/agent-admin/agent-app"

# mkdir -p (중간 경로까지 한 번에 생성)
mkdir -p $AGENT_HOME/upload_files
mkdir -p $AGENT_HOME/api_keys
mkdir -p $AGENT_HOME/bin
mkdir -p /var/log/agent-app

# ─────────────────────────────────────────
# [꼬리물기] 왜 /var/log 에 로그 폴더를 만들까?
# ─────────────────────────────────────────
# Linux의 FHS(파일시스템 계층 구조 표준) 규칙 때문임.
# - /home : 사용자 개인 파일
# - /etc  : 시스템 설정 파일
# - /var  : 가변 데이터 (로그, 임시 파일, DB 등)
# 로그는 계속 크기가 변하므로 /var/log/ 하위에 두는 것이 Linux 표준 관례.
```

### 5-2. 디렉토리 기본 소유권 설정

```bash
# 1) AGENT_HOME 전체
# 소유자: agent-admin, 그룹: agent-common
# -R = 하위 폴더까지 전부 적용
chown -R agent-admin:agent-common $AGENT_HOME

# 2) api_keys 폴더 (보안 구역)
# 소유자: agent-admin, 그룹: agent-core
chown agent-admin:agent-core $AGENT_HOME/api_keys

# 3) /var/log/agent-app (로그 구역)
# 소유자: root (생성자), 그룹: agent-core
chown root:agent-core /var/log/agent-app
```

### 5-3. 디렉토리 기본 권한(chmod) 설정

```bash
# 770 = rwxrwx---
# 소유자(rwx=7), 그룹(rwx=7), 나머지(---=0)
# 즉, 해당 그룹에 속한 사람만 읽고 쓰고 실행(폴더 진입)할 수 있음

chmod 770 $AGENT_HOME/upload_files
chmod 770 $AGENT_HOME/api_keys
chmod 770 /var/log/agent-app
```

### 5-4. ACL(접근 제어 목록) 적용 - 세밀한 통제

> 기본 `chmod`만으로는 부족할 때 사용하는 것이 `ACL`임.
> 새로 생성되는 파일에도 자동으로 같은 권한을 주기 위해 `default(d:)` 옵션을 함께 사용함.

```bash
# 1) upload_files (agent-common 그룹 R/W 가능)
# m = modify, g = group
setfacl -m g:agent-common:rwx $AGENT_HOME/upload_files
setfacl -m d:g:agent-common:rwx $AGENT_HOME/upload_files

# 2) api_keys (agent-core 그룹 R/W 가능, agent-common은 차단)
setfacl -m g:agent-core:rwx $AGENT_HOME/api_keys
setfacl -m d:g:agent-core:rwx $AGENT_HOME/api_keys
# 명시적 차단 (보험용)
setfacl -m g:agent-common:--- $AGENT_HOME/api_keys

# 3) /var/log/agent-app (agent-core 그룹 R/W 가능)
setfacl -m g:agent-core:rwx /var/log/agent-app
setfacl -m d:g:agent-core:rwx /var/log/agent-app

# 4) 설정 결과 확인
ls -la $AGENT_HOME
# drwxrwx---+  agent-admin agent-common  upload_files
# drwxrwx---+  agent-admin agent-core    api_keys
# (권한 끝에 붙은 '+' 기호가 ACL이 적용되었음을 의미함)

getfacl $AGENT_HOME/upload_files
getfacl $AGENT_HOME/api_keys

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ④
# ls -la $AGENT_HOME 의 출력 결과 (권한 부분과 '+' 기호가 잘 보이게)
# getfacl $AGENT_HOME/upload_files 결과
# getfacl $AGENT_HOME/api_keys 결과
# 세 가지를 스크린샷 캡처
# ────────────────────────────────
```

---

## STEP 6: 환경 변수 영구 등록 및 앱 준비

### 6-1. 사용자 전환

```bash
# 환경 변수는 시스템을 실행할 주체인 "agent-admin" 계정에 등록해야 함.
# 현재 root 계정에서 agent-admin으로 전환
su - agent-admin

# 잘 전환되었는지 확인
whoami
# agent-admin

pwd
# /home/agent-admin
```

### 6-2. ~/.bashrc 파일에 환경 변수 등록

```bash
# ~/.bashrc 파일은 로그인할 때마다 자동으로 실행되는 설정 파일임.
# 파일 끝에 환경 변수를 추가 (>> 리다이렉션 사용)

# cat >> 파일명 << 'EOF' 구조:
# 'EOF'라는 글자가 나올 때까지 내가 입력하는 모든 내용을 파일 끝에 추가하겠다는 뜻.

cat >> ~/.bashrc << 'EOF'

# ===== Agent App 환경 변수 =====
export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
export AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key
export AGENT_LOG_DIR=/var/log/agent-app
EOF

# 수정한 .bashrc 파일을 지금 즉시 적용
source ~/.bashrc

# 적용이 잘 되었는지 확인
env | grep AGENT
# 위에서 등록한 5개의 변수가 출력되어야 함!
```

### 6-3. 요구사항: 키 파일 생성

```bash
# 과제 요구사항: t_secret.key 파일을 생성하고 내용을 채운다.
# 경로는 위에서 설정한 $AGENT_KEY_PATH 를 그대로 사용.

# 1) 파일 생성 및 내용 입력
echo "agent_api_key_test" > $AGENT_KEY_PATH

# 2) 내용 확인
cat $AGENT_KEY_PATH
# agent_api_key_test

# 3) 파일 권한 제한
# 640 = rw-r----- (소유자 읽기/쓰기, 그룹 읽기만)
chmod 640 $AGENT_KEY_PATH

# 파일 상태 최종 확인
ls -la $AGENT_KEY_PATH
```

### 6-4. agent-app 바이너리 복사

> ⚠️ 이전에 Docker 컨테이너의 `/root` 폴더에 `agent-app`을 전송해 둔 상태임.
> `agent-admin` 계정은 `/root` 폴더에 들어갈 권한이 없으므로, 다시 `root`로 빠져나가서 복사해주어야 함.

```bash
# 1) agent-admin 계정에서 빠져나와 root로 복귀
exit

# 현재 root 계정인지 확인
whoami
# root

# 2) /root 에 있던 agent-app을 AGENT_HOME으로 복사
cp /root/agent-app /home/agent-admin/agent-app/agent-app

# 3) 파일 소유자를 agent-admin으로 변경
chown agent-admin:agent-admin /home/agent-admin/agent-app/agent-app

# 4) 실행 권한 부여
chmod +x /home/agent-admin/agent-app/agent-app

# 5) 모든 준비가 끝났으므로, 다시 agent-admin으로 접속하여 대기
su - agent-admin
cd $AGENT_HOME
ls -la
# agent-app 파일이 초록색(실행 가능 파일)으로 보여야 함.
```

---

## ✅ STEP 4~6 완료 체크리스트

```
□ root 계정에서 작업 시작했음
□ agent-admin, agent-dev, agent-test 3개 계정 생성 및 비밀번호 설정 완료
□ agent-common, agent-core 2개 그룹 생성 완료
□ usermod -aG 명령어로 각 계정을 알맞은 그룹에 추가 완료 (id 명령어로 확인)
□ 캡처 ③ 저장 완료
□ mkdir -p로 $AGENT_HOME 내부 폴더 3개와 /var/log/agent-app 폴더 생성 완료
□ chown, chmod, setfacl을 이용해 요구사항에 맞는 소유권/권한/ACL 설정 완료
□ 캡처 ④ 저장 완료
□ su - agent-admin 으로 계정 전환 완료
□ ~/.bashrc 파일에 export AGENT_... 5줄 추가 및 source ~/.bashrc 적용 완료
□ env | grep AGENT 명령어로 환경변수 5개 확인 완료
□ echo 명령어로 t_secret.key 파일 생성 완료
□ root 계정으로 잠시 돌아가서 agent-app 파일을 AGENT_HOME으로 복사하고 실행 권한(+x) 부여 완료
```

---
> 다음: 04_구현_STEP7~10.md (앱 실행, monitor.sh 개발, crontab 자동화)
