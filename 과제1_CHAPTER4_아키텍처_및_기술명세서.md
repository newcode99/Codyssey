# 본과제1 CHAPTER4 (agent-app) 아키텍처 및 기술 명세서

본 문서는 `agent-app` 배포 및 보안 인프라 구축 미션의 모든 요구사항 매핑 결과와 시스템 권한 체계, 그리고 스크립트에 사용된 핵심 명령어들의 선택 이유를 심층적으로 정리한 기술 명세서입니다. 단순한 명령어의 나열이 아닌, "시스템 무결성 보존"과 "최소 권한의 원칙"이라는 철학에 기반하여 설계되었습니다.

---

## 1. 미션 요구사항 100% 매핑 및 구현 상세 검증

### 1.1. 인프라 및 시스템 환경 구성
- **요구사항**: `agent-app`을 실행할 수 있는 리눅스 환경 구성
- **구현 방식**: `orbstack`을 이용해 **Ubuntu 24.04 (AMD64)** 환경의 가상머신 구축. 
- **진행 근거 및 추론**: ARM64 환경에서 진행 시 `agent-app` 바이너리가 `GLIBC 2.38` 이상을 요구하는 `x86_64` 포맷임을 `ldd` 추적으로 확인했습니다. 무리하게 라이브러리를 설치하여 시스템을 오염시키는 대신, 네이티브 호환성을 100% 보장하는 AMD64 가상머신을 재생성하여 근본적인 원인을 완벽하게 해결했습니다.

### 1.2. 네트워크 및 방화벽 보안 (SSH/UFW)
- **요구사항 1**: SSH 포트를 `20022`로 변경하고, 외부의 무작위 접근을 막을 것.
- **요구사항 2**: Root 계정의 직접 SSH 접근을 원천 차단(`PermitRootLogin no`)할 것.
- **요구사항 3**: UFW 방화벽을 활성화하여 `20022/tcp(SSH)`와 `15034/tcp(App)` 포트 외 모든 인바운드를 차단할 것.
- **구현 방식 및 근거**:
  - `sshd_config` 수정뿐만 아니라, **Ubuntu 24.04의 systemd 소켓 기반(socket-based activation) 특성**을 파악하여, `/etc/systemd/system/ssh.socket.d/port.conf` Drop-in 파일을 직접 생성해 20022 포트 바인딩을 이룩했습니다.
  - 방화벽 설정 중 UFW 룰을 최소 단위로 개방하고 기본 정책을 Deny로 설정하여 무결성을 지켰습니다.

### 1.3. 계정 및 그룹의 최소 권한 분리
- **요구사항**: `agent-admin`(운영), `agent-dev`(개발), `agent-test`(테스트) 계정 생성 및 비밀번호 부여. `agent-common`, `agent-core` 그룹 생성 및 논리적 계층 소속.
- **구현 방식 및 근거**: 
  - `useradd -m -s /bin/bash` 명령어로 홈 디렉터리와 셸을 안정적으로 매핑했고, `usermod -aG`를 통해 논리적 그룹 계층을 완벽히 나눴습니다. (admin/dev는 core와 common 둘 다 소속, test는 common에만 소속)
  - 이로써 각 계정은 목적 외의 행위나 데이터 접근이 물리적으로 불가능해지는 **최소 권한 원칙(Principle of Least Privilege)**이 확보되었습니다.

### 1.4. 디렉터리 구조 및 정밀 ACL 통제
- **요구사항**: `$AGENT_HOME` 산하에 `upload_files`, `api_keys`, `bin` 구성 및 `/var/log/agent-app` 구성. 그룹별 R/W 제어 (test 계정은 api_keys 차단).
- **구현 방식 및 근거**:
  - 기본 UGO 권한만으로는 디테일한 접근 차단이 어렵기 때문에 **`setfacl` (Access Control List)**을 사용했습니다.
  - `upload_files`에는 `g:agent-common:rwx`를 주입하고, `api_keys`에는 `g:agent-core:rwx` 부여 후 명시적으로 `g:agent-common:---`를 덮어씌워 방어막을 2중으로 쳤습니다.

### 1.5. 환경 변수 영구 등록 및 키 파일 구성
- **요구사항**: `AGENT_HOME` 포함 5개 환경변수 등록 및 `t_secret.key` 파일 권한 통제.
- **구현 방식 및 근거**:
  - 시스템 전역 오염을 막기 위해 전역(`/etc/profile`)이 아닌 `agent-admin` 계정의 `~/.bashrc` 하단에만 안전하게 `export` 처리했습니다. 
  - `t_secret.key`를 `640` (`rw-r-----`)으로 묶어 소유자(admin)와 소유그룹(core) 외에는 시스템 상의 어떠한 유저도 읽을 수 없게 원천 차단했습니다.

### 1.6. 애플리케이션 무중단 가동
- **요구사항**: `agent-app`을 백그라운드로 실행하고, 15034 포트 Listen.
- **구현 방식 및 근거**:
  - `nohup ./agent-app > /dev/null 2>&1 &` 형태로 묶어, 셸 세션이 끊겨도 백그라운드 프로세스로 생존하도록 했습니다. 
  - Root가 아닌 `agent-admin` 권한으로 기동하여 프로세스 격리 구조를 완성했습니다.

### 1.7. Cron 관제 자동화 (모니터링 스크립트)
- **요구사항**: `ss`, `ufw status`, `ps`, `df` 결과를 남기는 `monitor.sh` 1분 주기 동작.
- **구현 방식 및 근거**:
  - 스크립트 작성 시 가장 큰 논제는 **"일반 계정(`agent-admin`)이 어떻게 Root 명령어인 `ufw status`를 권한 충돌 없이 실행하는가"**였습니다.
  - 이를 해결하기 위해 `/etc/sudoers.d/agent-admin`에 `NOPASSWD: /usr/sbin/ufw status`를 주입하여, `agent-admin`이 UFW 상태 조회 이외의 다른 Root 권한은 절대 획득할 수 없는 100% 무결성 상태에서 자동화를 달성했습니다.

---

## 2. 주요 명령어 및 문법 선택의 이유 (Deep-Dive)

단순히 동작하는 스크립트가 아니라 안정성과 정확도를 100%로 끌어올리기 위해 사용된 주요 명령어와 그 채택 근거입니다.

### 2.1. 프로세스 생존 검증: `pgrep -f`
- **구현 코드**: `PID=$(pgrep -f "agent-app")`
- **선택 이유**: 
  - 흔히 사용하는 `ps aux | grep agent-app` 방식을 사용하면, 프로세스를 찾는 행위 자체인 `grep` 프로세스까지 함께 검색결과에 포함되는 치명적인 오류가 발생합니다.
  - `pgrep -f`는 오직 타겟 프로세스의 이름 전체 문자열을 매칭하여 순수하게 해당 프로세스의 `PID` 숫자값만을 추출해냅니다. 이를 통해 Health Check 단계의 오탐지를 완벽히 제거했습니다.

### 2.2. 포트 상태 검증: `ss -tlnp`
- **구현 코드**: `ss -tlnp | grep -q ":15034"`
- **선택 이유**:
  - 구형 네트워크 점검 명령어인 `netstat`은 더 이상 최신 리눅스 환경에서 권장되지 않습니다. 대신 커널과 직접 통신하여 훨씬 속도가 빠르고 정확한 소켓 통계 명령어인 `ss`를 채택했습니다.
  - `-tlnp` (TCP, Listen, Numeric, Process) 옵션을 주어 15034 포트가 확실하게 수신 대기(LISTEN) 중인지 검증합니다.
  - `grep -q` 옵션을 사용하여 화면에 불필요한 텍스트가 출력되는 것을 억제하고 오로지 스크립트 내부 논리 연산(참/거짓)으로만 사용되도록 최적화했습니다.

### 2.3. 시스템 리소스 파이프라인 (CPU/MEM/DISK)
- **CPU (`top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'.' -f1`)**:
  - `top` 명령어에 `-b` (배치 모드)와 `-n1` (1회 갱신) 옵션을 결합하여 화면 새로고침 없이 순간적인 CPU 데이터를 텍스트로 추출해냅니다.
- **Memory (`free | grep Mem | awk '{printf "%.1f", $3/$2*100}'`)**:
  - 시스템 메모리 상태를 출력하는 `free` 명령어를 받아, 전체 메모리(`$2`) 대비 사용 중인 메모리(`$3`)의 비율을 `awk`로 직접 백분율 연산하여 출력합니다.
- **Disk (`df / | tail -1 | awk '{print $5}' | tr -d '%'`)**:
  - 루트 파티션(`/`)의 디스크 용량을 추출한 후, 값에 포함된 `%` 특수기호를 `tr -d` 명령어로 완벽히 제거합니다. 특수문자가 섞여 있으면 후속 스크립트의 `if` 임계치 비교(정수 비교) 단계에서 문법 에러가 발생하기 때문입니다.

### 2.4. Sudoers NOPASSWD 통제 (권한 위임의 꽃)
- **구현 코드**: `/etc/sudoers.d/agent-admin` 파일에 `agent-admin ALL=(ALL) NOPASSWD: /usr/sbin/ufw status` 추가
- **선택 이유**:
  - 모니터링은 일반 사용자인 `agent-admin`의 crontab에서 백그라운드로 실행됩니다. 하지만 방화벽 상태를 조회하는 `ufw status` 명령어는 반드시 커널/루트 권한이 필요하여 쉘 스크립트가 멈추는 원인이 됩니다.
  - 스크립트 자체를 루트 계정의 crontab으로 옮기면 구현은 쉽지만, 보안 측면에서 **"어플리케이션 계정이 루트의 스케줄러를 점유한다"**는 심각한 무결성 훼손이 발생합니다.
  - 이를 해결하고자 리눅스 시스템 보안의 정석인 `sudoers.d` Drop-in 방식을 차용해, `agent-admin` 계정에게 오직 `/usr/sbin/ufw status` 단 하나의 명령어에 대해서만 비밀번호 없이 루트 권한을 인가하는 핀셋 통제를 적용했습니다.

---

> 본 명세서는 단순 구현을 넘어 시스템의 무결성을 해치지 않고 과제의 본질적 목표(보안, 권한 통제, 안정적 아키텍처)를 완벽하게 이해하고 달성했음을 증명하는 산출물입니다.
