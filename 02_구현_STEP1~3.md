# 본과제 1 — Chapter 02: 구현 STEP 1~3
## 환경 준비 + SSH 보안 설정 + 방화벽 설정

> ⚠️ **시작 전 필수 확인**
> agent-app은 Linux ELF 실행 파일 → Mac에서 직접 실행 불가
> Ubuntu 22.04 환경 먼저 구성 필요

---

## STEP 1: Ubuntu 환경 구성

### 1-1. 환경 선택

```
[선택지와 이유]

Docker (권장):
→ 가장 빠르게 Ubuntu 환경 시작 가능
→ 과제 종료 후 컨테이너 삭제로 깨끗한 정리
→ Mac, Windows 모두 지원
→ agent-app 파일 전송 명령어 1줄로 가능

VirtualBox + Ubuntu ISO:
→ 가장 완전한 Linux 경험
→ 설치 시간 30분 이상 소요
→ 과제 환경과 가장 유사

선택: Docker 권장 (속도와 편의성)
```

### 1-2. Docker 설치 및 컨테이너 시작

```bash
# Mac 터미널에서 실행

# Docker 설치 여부 확인
docker --version
# Docker version 24.x.x ← 이런 형태면 설치된 것
# "command not found" 면 → https://www.docker.com/products/docker-desktop 에서 설치

# Ubuntu 22.04 컨테이너 시작
docker run -it \
  --name agent-server \
  --hostname agent-server \
  -p 20022:20022 \
  -p 15034:15034 \
  ubuntu:22.04 \
  /bin/bash

# 명령어 해설:
# docker run     = 컨테이너 생성 및 실행
# -it            = 대화형 터미널 연결
#                  -i: 표준 입력 유지
#                  -t: 가상 터미널 할당
# --name         = 컨테이너 이름 (나중에 재접속 시 사용)
# --hostname     = 컨테이너 내부 호스트명
# -p 20022:20022 = 포트 포워딩 (Mac의 20022 → 컨테이너의 20022)
# -p 15034:15034 = 포트 포워딩 (Mac의 15034 → 컨테이너의 15034)
# ubuntu:22.04   = 사용할 Docker 이미지
# /bin/bash      = 컨테이너 시작 시 실행할 명령어

# 실행 후 프롬프트가 바뀜:
# root@agent-server:/#   ← 이제 Ubuntu 안에 있음
```

### 1-3. agent-app 파일 전송

```bash
# ⚠️ 새 Mac 터미널 창을 열어서 실행 (Docker 컨테이너 밖에서)

docker cp \
  /Users/hwangjeonghyeon/practice/python-practice/B/01/agent-app \
  agent-server:/root/agent-app

# 명령어 해설:
# docker cp      = 호스트 ↔ 컨테이너 파일 복사
# 첫 번째 인자   = Mac에서의 파일 경로
# 두 번째 인자   = 컨테이너명:컨테이너 내부 경로

# 전송 확인 (Ubuntu 컨테이너 안에서)
ls -la /root/agent-app
# -rw-r--r-- 1 root root 7926296 ... agent-app
# 파일이 있어야 함
```

### 1-4. 필수 도구 설치

```bash
# Ubuntu 컨테이너 안에서 실행

# 패키지 목록 업데이트
apt update

# 필수 도구 설치
apt install -y \
  sudo \
  openssh-server \
  ufw \
  acl \
  net-tools \
  procps \
  nano \
  curl \
  systemctl

# 각 도구 설명:
# sudo          = 관리자 권한 명령 실행 도구
# openssh-server = SSH 서버 프로그램
# ufw           = 방화벽 (Uncomplicated Firewall)
# acl           = 파일 ACL 권한 설정 도구
# net-tools     = ss, netstat 등 네트워크 도구
# procps        = ps, top 등 프로세스 도구
# nano          = 텍스트 편집기 (초보자 친화적)

# 설치 완료 확인
which ufw && which ssh && echo "설치 완료"
```

---

## STEP 2: SSH 보안 설정

### 2-1. SSH 서비스 시작

```bash
# Ubuntu 컨테이너 안에서

# SSH 서비스 시작
service ssh start
# * Starting OpenBSD Secure Shell server sshd   [ OK ]

# SSH 서비스 상태 확인
service ssh status
# sshd is running  ← 이 줄이 보여야 정상
```

### 2-2. SSH 설정 파일 수정

```bash
# 설정 파일 위치: /etc/ssh/sshd_config
# sshd = SSH Daemon (백그라운드에서 연결 대기하는 프로그램)
# config = 이 프로그램의 설정 파일

# 현재 설정 확인 (수정 전)
grep -E "^#?Port|^#?PermitRootLogin" /etc/ssh/sshd_config

# 원본 백업 (중요! 실수 시 복원 가능)
cp /etc/ssh/sshd_config /etc/ssh/sshd_config.backup

# 설정 파일 편집
nano /etc/ssh/sshd_config

# ──── nano 사용법 ────
# Ctrl+W       = 검색
# 방향키       = 이동
# 직접 수정    = 타이핑
# Ctrl+X       = 종료
# Y            = 저장 확인
# Enter        = 파일명 유지 (기본값으로 저장)
```

**수정 내용 (2곳):**

```
[찾을 내용]              [바꿀 내용]
#Port 22          →     Port 20022

#PermitRootLogin  →     PermitRootLogin no
prohibit-password
```

```bash
# 저장 후 SSH 서비스 재시작 (새 설정 적용)
service ssh restart

# 적용 확인
grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
# Port 20022          ← 이 줄
# PermitRootLogin no  ← 이 줄
# 두 줄 모두 나와야 함

# 포트 리슨 상태 확인
ss -tlnp | grep sshd
# tcp LISTEN 0 128 0.0.0.0:20022 ... sshd
# 20022 포트에서 대기 중임을 확인

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ①
# 아래 두 명령어 결과를 스크린샷 저장:
grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
ss -tlnp | grep sshd
# ────────────────────────────────
```

---

## STEP 3: UFW 방화벽 설정

### 3-1. 방화벽 설정 순서가 왜 중요한가?

```
[잘못된 순서로 하면 발생하는 문제]

1. ufw enable 먼저 실행 → 모든 포트 차단
2. 20022 포트 허용 추가 시도
→ 문제: SSH가 이미 차단됨 → 명령 실행 불가!
→ 서버에 접속 자체가 불가능해짐 (Lock-out)

[올바른 순서]
1. 포트 허용 규칙 먼저 등록 (방화벽 OFF 상태에서)
2. 방화벽 활성화
3. 상태 확인
```

### 3-2. UFW 설정

```bash
# Ubuntu 컨테이너 안에서

# 1단계: SSH 포트 허용 (먼저!)
ufw allow 20022/tcp comment 'SSH Custom Port'

# 명령어 해설:
# ufw allow     = 해당 포트 통과 허용
# 20022/tcp     = 포트번호 / 프로토콜(TCP)
# comment '...' = 이 규칙의 설명 (나중에 ufw status에서 표시)

# 2단계: 앱 포트 허용
ufw allow 15034/tcp comment 'Agent App Port'

# 3단계: 방화벽 활성화
ufw enable
# "Command may disrupt existing ssh connections. Proceed with operation (y|n)?"
# → y 입력 후 Enter

# 4단계: 상태 확인
ufw status verbose

# 예상 출력:
# Status: active
# Logging: on (low)
# Default: deny (incoming), allow (outgoing)
# To                         Action      From
# --                         ------      ----
# 20022/tcp                  ALLOW IN    Anywhere    (SSH Custom Port)
# 15034/tcp                  ALLOW IN    Anywhere    (Agent App Port)

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ②
# ufw status verbose 결과 스크린샷 저장
# ────────────────────────────────
```

### 3-3. Docker 환경에서 UFW가 안 될 경우

```bash
# 오류: "ERROR: Could not load logging rules"
# 원인: Docker 컨테이너는 완전한 커널을 갖지 않아 UFW 제한

# 해결책 1: iptables 직접 사용
iptables -A INPUT -p tcp --dport 20022 -j ACCEPT
iptables -A INPUT -p tcp --dport 15034 -j ACCEPT

# 해결책 2: ufw --force enable
ufw --force enable

# 해결책 3: VirtualBox 환경에서 진행
# → 완전한 Linux 커널 환경에서는 UFW 정상 동작

# Docker 환경에서 방화벽 상태 대안 확인
iptables -L INPUT -n | grep -E "20022|15034"
```

---

## STEP 1~3 완료 체크리스트

```
□ Docker 컨테이너 실행 중 (docker ps 확인)
□ agent-app 파일 컨테이너로 전송 완료
□ apt update + 필수 도구 설치 완료
□ SSH 서비스 실행 중 (service ssh status)
□ Port 20022 설정 확인
□ PermitRootLogin no 설정 확인
□ UFW 활성화 (Status: active)
□ 20022/tcp 허용 확인
□ 15034/tcp 허용 확인
□ 스크린샷 ① (SSH 설정) 저장
□ 스크린샷 ② (UFW 상태) 저장
```

---

> 다음: 03_구현_STEP4~6.md (계정/그룹/권한/환경변수)
