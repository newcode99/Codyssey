# 본과제 1 — Chapter 05: 최종 검증 및 오류 해결
## 제출 전 최종 체크리스트 & Troubleshooting

> 이 문서는 과제의 모든 구현을 끝낸 후 제출물(수행 내역서 문서, monitor.sh 코드)을 취합하기 전에 마지막으로 확인하는 용도입니다.

---

## 5-1. 최종 제출물 요약

| 구분 | 형태 | 내용 |
|---|---|---|
| **제출물 1** | 문서 (PDF/Word/Markdown) | **요구사항 수행 내역서** (아래 8개의 필수 스크린샷 포함) |
| **제출물 2** | 소스코드 (`monitor.sh`) | Chapter 04 에서 작성한 자동 관제 스크립트 파일 |

---

## 5-2. 수행 내역서 필수 증거 자료 (8가지) 체크리스트

문서를 작성할 때 아래 8가지 캡처가 모두 들어갔는지 순서대로 확인하십시오.
각 항목 위에 설명(어떤 설정을 했고, 어떤 명령어를 쳤는지)을 한두 줄씩 적어주면 더욱 완벽합니다.

- [ ] **① SSH 포트 변경 & Root 접속 차단 설정 확인**
  - 명령어: `grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config`
  - 확인점: `Port 20022`, `PermitRootLogin no` 출력 보임
- [ ] **② 방화벽(UFW) 활성화 및 허용 포트 확인**
  - 명령어: `sudo ufw status verbose` (또는 `ufw status`)
  - 확인점: `Status: active`, `20022/tcp ALLOW IN`, `15034/tcp ALLOW IN` 출력 보임
- [ ] **③ 역할 기반 계정 & 그룹 생성 확인**
  - 명령어: `id agent-admin`, `id agent-dev`, `id agent-test` (세 가지 모두)
  - 확인점: 각 계정의 `groups=` 항목에 올바른 그룹(`agent-common`, `agent-core`)이 할당되어 있음
- [ ] **④ 디렉토리 구조 및 세밀한 권한(ACL) 설정 확인**
  - 명령어: `ls -la /home/agent-admin/agent-app/` 및 `getfacl /home/agent-admin/agent-app/upload_files` 등
  - 확인점: 디렉토리 권한 끝에 `+` 기호가 붙어있으며, `getfacl` 출력에 그룹별 권한이 올바르게 나타남
- [ ] **⑤ 애플리케이션 Boot Sequence 5단계 [OK] 확인**
  - 명령어: `agent-app` 실행
  - 확인점: `[1/5]` ~ `[5/5]` 까지 모두 `[OK]`가 떨어지고, 제일 밑에 `Agent READY`가 출력됨
- [ ] **⑥ monitor.sh 수동 실행 결과 (프로세스/포트/리소스/경고)**
  - 명령어: `/home/agent-admin/agent-app/bin/monitor.sh`
  - 확인점: `[HEALTH CHECK]`, `[FIREWALL CHECK]`, `[RESOURCE MONITORING]`, `[WARNING]` (해당 시), `[INFO] Log appended...` 가 잘 출력됨
- [ ] **⑦ monitor.log 누적 기록 확인**
  - 명령어: `tail -n 10 /var/log/agent-app/monitor.log`
  - 확인점: 로그 포맷(`[시간] PID:... CPU:... MEM:... DISK_USED:...`)대로 데이터가 기록되어 있음
- [ ] **⑧ crontab 등록 및 자동 실행 확인**
  - 명령어: `crontab -l`
  - 확인점: `* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /tmp/cron_err.log 2>&1` 이 등록되어 있고, ⑦번 스크린샷과 연계하여 1분 간격으로 로그가 찍혔음이 증명됨

---

## 5-3. 자주 발생하는 오류 및 해결법 (Troubleshooting)

과제를 수행하다가 문제가 생겼을 때, 당황하지 말고 아래 항목을 확인하십시오.
이 과정 자체가 현업 엔지니어가 장애를 추적하는 과정과 똑같습니다.

### 🔴 문제 1: SSH 20022번 포트로 접속이 안 돼요 (연결 거부)
- **원인 분석**: `sshd_config`를 수정하고 나서 SSH 서비스를 재시작하지 않았거나, 방화벽(UFW)에서 20022 포트를 허용하지 않은 채 방화벽을 켰을 확률이 높습니다.
- **해결책**:
  1. `service ssh restart` 명령어 실행.
  2. `ss -tlnp | grep sshd`를 쳐서 20022 포트가 열렸는지 확인.
  3. `sudo ufw status`를 쳐서 20022가 `ALLOW` 상태인지 확인.

### 🔴 문제 2: agent-app 을 실행했는데 Boot Sequence [1/5] 에서 FAIL 이 나요
- **원인 분석**: `root` 계정으로 앱을 실행했기 때문입니다. (과제 제약 조건: 루트 실행 금지)
- **해결책**: 
  1. 현재 계정이 무엇인지 `whoami` 로 확인.
  2. `root` 라면 `su - agent-admin` 을 입력하여 계정을 변경한 뒤 다시 실행.

### 🔴 문제 3: agent-app Boot Sequence [2/5] 환경변수 단계에서 FAIL 이 나요
- **원인 분석**: 환경 변수 5개(`AGENT_HOME`, `AGENT_PORT` 등)가 현재 쉘에 로드되지 않았습니다.
- **해결책**:
  1. `env | grep AGENT` 명령을 쳐서 변수가 아무것도 안 나온다면 로드 안 된 것임.
  2. `cat ~/.bashrc` 를 쳐서 맨 아래에 환경변수 `export` 구문이 잘 적혀 있는지 확인.
  3. 오타가 없다면 `source ~/.bashrc` 를 입력하여 즉시 적용.

### 🔴 문제 4: monitor.sh 를 실행했는데 "Permission denied (권한 거부)" 가 뜹니다.
- **원인 분석**: 스크립트 파일에 "실행(execute)" 권한이 없거나, 현재 계정이 그 파일을 실행할 그룹(agent-core)에 속해있지 않기 때문입니다.
- **해결책**:
  1. `ls -l $AGENT_HOME/bin/monitor.sh` 로 권한 확인. (앞부분이 `-rwxr-x---` 이어야 함)
  2. `chmod 750 $AGENT_HOME/bin/monitor.sh` 로 권한 재설정.
  3. `id` 명령어를 쳐서 현재 계정이 `agent-core` 그룹에 들어있는지 확인.

### 🔴 문제 5: cron을 등록했는데 1분이 지나도 로그 파일에 내용이 안 쌓여요.
- **원인 분석**: cron 데몬 서비스가 죽어있거나, 스크립트 내부에서 사용한 명령어 경로를 시스템(cron)이 못 찾고 있을 수 있습니다.
- **해결책**:
  1. `service cron status` (또는 `service crond status`)로 cron 서비스가 `running` 상태인지 확인. 죽어있다면 `service cron start`.
  2. 에러 로그 확인: `cat /tmp/cron_err.log` (우리가 crontab에 등록할 때 에러를 여기로 빼도록 설정해 두었음).
  3. 에러 내용에 `command not found` (ex: `ss: command not found`) 가 뜬다면, 스크립트 내부 명령어들의 경로 환경변수 문제임. 스크립트 파일(`monitor.sh`) 맨 위(`#!/bin/bash` 바로 아래)에 `PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin` 한 줄을 추가해줌.

---
**🎉 축하합니다! 리눅스 기반 시스템 보안 설정 및 관제 자동화 시스템 구축을 성공적으로 완료하였습니다.**
이 경험은 향후 DevOps, 인프라 엔지니어링, 백엔드 개발 등 모든 서버 관련 직무에서 강력한 기초 자산이 될 것입니다.
