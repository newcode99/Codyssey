# 본과제 1 — Chapter 04: 구현 STEP 7~10
## 앱 실행 + monitor.sh 구현 + crontab 자동화

> ⚠️ **시작 전 필수 확인**
> 지금 터미널 창의 프롬프트가 `agent-admin` 계정인지 확인.
> (`root` 계정에서 실행하면 과제 요구사항 "루트 실행 금지" 위반으로 감점됨)
> 만약 `root`라면 `su - agent-admin` 명령어로 전환할 것.

---

## STEP 7: 애플리케이션(agent-app) 실행

### 7-1. 실행 및 Boot Sequence 확인

```bash
# agent-admin 계정 확인
whoami
# agent-admin

# 디렉토리 이동
cd $AGENT_HOME

# 앱을 백그라운드(&)에서 실행
./agent-app &

# ─────────────────────────────────────────
# [꼬리물기] 뒤에 붙은 '&' 기호는 무엇인가요?
# ─────────────────────────────────────────
# 프로그램 실행 시 맨 뒤에 &를 붙이면 "백그라운드 실행"이 됨.
# 앱이 터미널 화면을 독점하지 않고 뒤에서 조용히 돌게 만들어서,
# 우리는 계속해서 다른 명령어(monitor.sh 작성 등)를 입력할 수 있게 함.
```

정상적으로 모든 환경 변수와 권한이 설정되었다면, 화면에 다음과 같이 **Boot Sequence 5단계**가 출력됨:

```
> Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
... Running as service user 'agent-admin' (uid=1001)
[2/5] Verifying Environment Variables     [OK]
... All required Envs correct
[3/5] Checking Required Files             [OK]
... Verified key file with correct key string.
[4/5] Checking Port Availability          [OK]
... Port 15034 is available.
[5/5] Verifying Log Permission            [OK]
... Log directory is writable: /var/log/agent-app
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
```

> **만약 에러([FAIL])가 뜬다면?**
> - `[1/5]` 에러: 현재 계정이 `agent-admin`인지 확인.
> - `[2/5]` 에러: `env | grep AGENT` 로 변수 5개가 모두 제대로 나오는지 확인.
> - `[3/5]` 에러: `cat $AGENT_KEY_PATH` 로 내용이 `agent_api_key_test` 인지 확인.
> - `[4/5]` 에러: 포트가 이미 사용 중. `ss -tlnp | grep 15034` 후 프로세스 강제 종료(`kill -9 PID`).
> - `[5/5]` 에러: 로그 디렉토리 권한 문제. root 계정에서 `setfacl` 제대로 했는지 확인.

```bash
# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ⑤
# 화면에 출력된 Boot Sequence 5단계 전체와 "Agent READY" 문구를 캡처.
# ────────────────────────────────
```

### 7-2. 앱 정상 동작 확인 (포트 LISTEN)

```bash
# 앱이 정상적으로 15034번 포트를 열고 대기 중인지 네트워크 상태 확인
ss -tlnp | grep 15034

# 예상 출력:
# tcp   LISTEN 0      128          0.0.0.0:15034      0.0.0.0:*    users:(("agent-app",pid=1234,fd=3))
# (앱이 15034번 창구를 열어두고 손님을 기다리는 상태)
```

---

## STEP 8: 시스템 관제 스크립트 (monitor.sh) 작성

> 시스템의 상태(프로세스 생존 여부, 포트, CPU/메모리/디스크 등)를 검사하고
> 로그를 남기는 핵심 스크립트를 작성함.

### 8-1. 빈 파일 생성 및 편집

```bash
# $AGENT_HOME/bin 폴더 안에 monitor.sh 생성
nano $AGENT_HOME/bin/monitor.sh
```

### 8-2. monitor.sh 전체 소스 코드

아래 내용을 복사하여 `nano` 에디터 화면에 우클릭(또는 Shift+Insert)으로 붙여넣기.

```bash
#!/bin/bash
# ==============================================================================
# monitor.sh - 시스템 상태 수집 및 로깅 스크립트
# 요구사항: Health Check, 방화벽 점검, 리소스 수집(CPU/MEM/DISK), 경고 출력, 로깅
# 소유자: agent-dev | 그룹: agent-core | 권한: 750
# ==============================================================================

# --- [변수 설정] ---
# cron으로 실행될 때는 사용자의 환경 변수(.bashrc)를 읽지 못할 수 있으므로,
# 스크립트 내부에서 핵심 경로를 명시적으로 선언해 주는 것이 매우 안전함.
AGENT_APP_NAME="agent-app"
AGENT_APP_PORT=15034
LOG_FILE="/var/log/agent-app/monitor.log"
LOG_MAX_SIZE=$((10 * 1024 * 1024))  # 10MB
LOG_MAX_FILES=10
WARN_CPU=20
WARN_MEM=10
WARN_DISK=80

# ==============================================================================
# 1. Health Check (실패 시 즉시 종료 exit 1)
# ==============================================================================
echo "====== SYSTEM MONITOR RESULT ======"
echo ""
echo "[HEALTH CHECK]"

# 1-1. 프로세스 확인
# pgrep -f 옵션으로 해당 이름을 가진 프로세스의 PID 추출
PID=$(pgrep -f "$AGENT_APP_NAME")
if [ -z "$PID" ]; then
    echo "[ERROR] 프로세스 '$AGENT_APP_NAME' 실행 중지 상태."
    exit 1
fi
echo "Checking process '$AGENT_APP_NAME'... [OK] (PID: $PID)"

# 1-2. 포트 LISTEN 확인
# ss 출력에서 :15034 가 있는지 검사. -q 는 화면 출력 생략(결과만 참거짓으로 판단)
if ! ss -tlnp | grep -q ":$AGENT_APP_PORT"; then
    echo "[ERROR] 포트 $AGENT_APP_PORT 닫힘 상태."
    exit 1
fi
echo "Checking port $AGENT_APP_PORT... [OK]"


# ==============================================================================
# 2. 상태 점검 (방화벽 - 실패 시 경고만 출력, 스크립트 계속 진행)
# ==============================================================================
echo ""
echo "[FIREWALL STATUS]"
# ufw status 명령 결과 중 'active' 단어가 있는지 검사
if command -v ufw &> /dev/null; then
    UFW_STATUS=$(sudo ufw status 2>/dev/null | grep -i "Status" | awk '{print $2}')
    if [ "$UFW_STATUS" = "active" ]; then
        echo "[OK] 방화벽(UFW) 활성화 상태"
    else
        echo "[WARNING] 방화벽(UFW) 비활성 상태!"
    fi
else
    echo "[WARNING] UFW 명령어를 찾을 수 없음 (Docker 환경 제약일 수 있음)"
fi


# ==============================================================================
# 3. 자원 수집 (CPU / Memory / Disk)
# ==============================================================================
echo ""
echo "[RESOURCE MONITORING]"

# CPU 사용률 추출 (top 명령어 1회 실행 후 파싱)
CPU_USAGE=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'.' -f1)
if [ -z "$CPU_USAGE" ]; then CPU_USAGE=0; fi

# MEM 사용률 추출 (free 명령어 파싱, 소수점 1자리)
MEM_USAGE=$(free | grep Mem | awk '{printf "%.1f", $3/$2*100}')
if [ -z "$MEM_USAGE" ]; then MEM_USAGE=0.0; fi

# DISK 사용률 추출 (루트 파티션 기준, % 문자 제거)
DISK_USAGE=$(df / | tail -1 | awk '{print $5}' | tr -d '%')
if [ -z "$DISK_USAGE" ]; then DISK_USAGE=0; fi

echo "CPU Usage : ${CPU_USAGE}%"
echo "MEM Usage : ${MEM_USAGE}%"
echo "DISK Used : ${DISK_USAGE}%"


# ==============================================================================
# 4. 임계값 경고 출력
# ==============================================================================
echo ""
# 정수형으로 비교하기 위해 메모리는 소수점 버림
MEM_USAGE_INT=$(echo "$MEM_USAGE" | cut -d'.' -f1)

if [ "$CPU_USAGE" -gt "$WARN_CPU" ]; then
    echo "[WARNING] CPU threshold exceeded (${CPU_USAGE}% > ${WARN_CPU}%)"
fi

if [ "$MEM_USAGE_INT" -gt "$WARN_MEM" ]; then
    echo "[WARNING] MEM threshold exceeded (${MEM_USAGE}% > ${WARN_MEM}%)"
fi

if [ "$DISK_USAGE" -gt "$WARN_DISK" ]; then
    echo "[WARNING] DISK threshold exceeded (${DISK_USAGE}% > ${WARN_DISK}%)"
fi


# ==============================================================================
# 5. 로그 기록 및 용량 관리 (Log Rotation)
# ==============================================================================
# 파일이 10MB 초과 시 기존 로그를 .1, .2 형태로 미루고 새로 기록
if [ -f "$LOG_FILE" ]; then
    FILE_SIZE=$(stat -c%s "$LOG_FILE" 2>/dev/null || echo 0)
    if [ "$FILE_SIZE" -gt "$LOG_MAX_SIZE" ]; then
        for i in $(seq $((LOG_MAX_FILES - 1)) -1 1); do
            [ -f "${LOG_FILE}.${i}" ] && mv "${LOG_FILE}.${i}" "${LOG_FILE}.$((i + 1))"
        done
        mv "$LOG_FILE" "${LOG_FILE}.1"
        touch "$LOG_FILE"
    fi
fi

# 로그 라인 생성 및 파일 어펜드 (>>)
# 포맷: [YYYY-MM-DD HH:MM:SS] PID:... CPU:..% MEM:..% DISK_USED:..%
NOW=$(date "+%Y-%m-%d %H:%M:%S")
LOG_TEXT="[$NOW] PID:$PID CPU:${CPU_USAGE}% MEM:${MEM_USAGE}% DISK_USED:${DISK_USAGE}%"

echo "$LOG_TEXT" >> "$LOG_FILE"

echo ""
echo "[INFO] Log appended: $LOG_FILE"
echo "==================================="
```

복사 후: `Ctrl+X` → `Y` → `Enter` 로 저장.

---

## STEP 9: monitor.sh 파일 소유권 및 권한 설정

과제 요구사항:
- 소유자: `agent-dev` (스크립트 작성자)
- 그룹: `agent-core`
- 권한: `750` (`rwxr-x---`)

### 9-1. 소유자 및 권한 부여

> ⚠️ 이 작업은 `root` 권한이 필요하므로 `agent-admin` 계정에서 `sudo`를 사용하거나 잠시 `root`로 빠져나가서 작업함. (여기서는 sudo 사용)

```bash
# 소유자와 그룹 변경 (sudo 암호 입력 요구 시, agent-admin 가입 시 입력한 암호 사용)
sudo chown agent-dev:agent-core $AGENT_HOME/bin/monitor.sh

# 권한 750 (소유자는 모두 가능, 그룹은 읽기/실행, 나머지는 불가)
sudo chmod 750 $AGENT_HOME/bin/monitor.sh

# 권한 확인
ls -l $AGENT_HOME/bin/monitor.sh
# -rwxr-x--- 1 agent-dev agent-core ... monitor.sh 
# ↑ 정확히 이 모습이어야 함
```

### 9-2. 수동 실행 테스트 (가장 중요)

```bash
# 지금 우리는 agent-admin 계정임.
# agent-admin 계정은 agent-core 그룹에 속해 있으므로 750 권한에 따라 실행이 가능해야 함.

$AGENT_HOME/bin/monitor.sh

# 결과 화면에 SYSTEM MONITOR RESULT 가 예쁘게 출력되고, 오류 없이 끝나야 함.
# (CPU 20% 초과 등의 [WARNING]은 정상 동작임!)

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ⑥
# 위 명령어를 쳐서 화면에 출력된 전체 결과(Health Check, Resource, Warning 등) 캡처.
# ────────────────────────────────

# 로그가 잘 찍혔는지 파일 끝부분(tail) 확인
tail -n 5 /var/log/agent-app/monitor.log

# 출력 예시:
# [2026-05-14 16:30:05] PID:1234 CPU:5% MEM:6.2% DISK_USED:25%
```

---

## STEP 10: crontab 자동 실행 (자동화의 완성)

### 10-1. crontab 등록

```bash
# 현재 agent-admin 계정 상태에서 crontab 에디터 열기
crontab -e

# (최초 실행 시 텍스트 에디터 선택 화면이 나옴 → 1번 nano 선택 후 Enter)

# 파일의 맨 끝으로 커서를 이동시켜서 아래 내용 추가:
# (매분마다 스크립트를 실행하고, 혹시 모를 에러가 발생하면 /tmp/cron_err.log에 남겨라)
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /tmp/cron_err.log 2>&1

# 저장: Ctrl+X → Y → Enter
# 화면에 "crontab: installing new crontab" 메시지가 뜨면 성공.
```

### 10-2. 등록 확인 및 동작 대기

```bash
# 등록된 내용 확인
crontab -l
# 화면 맨 아래에 입력한 '* * * * * ...' 줄이 보여야 함.

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ⑧-A
# crontab -l 입력 후 나온 화면 캡처
# ────────────────────────────────

# 이제 1분~2분 정도 멍때리며 기다립니다.
# 스마트폰으로 알람 시계를 1분 맞춰놓아도 좋음.

sleep 65
```

### 10-3. 자동 로그 누적 확인

```bash
# 시간이 지났다면 로그 파일을 열어봄
tail -n 5 /var/log/agent-app/monitor.log

# 아까 내가 수동으로 실행했던 로그 밑에, 
# 1분 간격으로 시간이 찍힌 새로운 로그가 추가되어 있어야 함!
# 예:
# [2026-05-14 16:30:05] PID:1234 CPU:5% MEM:6.2% DISK_USED:25%  ← 아까 수동
# [2026-05-14 16:31:01] PID:1234 CPU:6% MEM:6.2% DISK_USED:25%  ← cron 자동
# [2026-05-14 16:32:01] PID:1234 CPU:4% MEM:6.2% DISK_USED:25%  ← cron 자동

# ────────────────────────────────
# ✅ 수행 내역서 캡처 포인트 ⑦ & ⑧-B
# 위 로그가 1분 간격으로 여러 줄 찍혀있는 화면을 캡처!
# (이것으로 "자동 관제 시스템이 완성되었다"는 것을 증명함)
# ────────────────────────────────
```

---

## 🎉 모든 과제 요구사항 구현 완료!
> 다음은 마지막 챕터: 05_검증_및_오류해결.md 에서 체크리스트를 점검하고 문서를 마무리함.
