# 본과제 1 — Chapter 01: 핵심 개념 완전 이해
> 이 파일을 읽고 나면 과제에서 나오는 모든 개념을 설명할 수 있어야 함.
> 지식 0% 기준으로 작성. 모든 개념은 "왜?" 부터 시작.

---

## 01-1. Linux란 무엇인가?

### 운영체제(OS)의 역할

```
컴퓨터의 계층 구조:

┌──────────────────────────────┐
│         사람 (사용자)          │  ← 명령을 내리는 존재
├──────────────────────────────┤
│     Shell (Bash 터미널)        │  ← 사람의 말을 컴퓨터 언어로 번역
├──────────────────────────────┤
│   운영체제 (Linux 커널)         │  ← 하드웨어를 제어하는 관리자
├──────────────────────────────┤
│  하드웨어 (CPU/메모리/디스크)   │  ← 실제 작업을 수행하는 장치
└──────────────────────────────┘

비유: 운영체제 = 공장 관리자
→ 작업자(하드웨어)에게 "이 계산 해줘", "이 파일 저장해줘" 지시
→ 여러 프로그램이 동시에 작업 요청해도 조율
```

### 왜 서버에는 Linux인가?

```
비교 항목      Windows Server    Linux
────────────────────────────────────────
가격           수백만 원         무료 (오픈소스)
안정성         재부팅 자주 필요   몇 년 무중단 운영 가능
자원 사용량    GUI 포함 시 무거움  최소 128MB RAM으로 동작
보안 제어      제한적            파일 하나하나 권한 설정
서버 점유율    ~4%              ~96%

결론: 싸고, 안정적이고, 가볍고, 보안 제어가 세밀함
→ 전 세계 서버의 96%가 Linux 사용
```

---

## 01-2. 터미널과 기본 명령어

### 터미널이 무엇인가?

```
Windows 탐색기:          Linux 터미널:
┌──────────────┐        $ ls /home
│ 📁 내 문서    │        agent-admin  agent-dev  agent-test
│ 📁 다운로드   │        $ cd /home/agent-admin
│ 📄 readme.txt│        $ mkdir myproject
└──────────────┘        $ ls
마우스 클릭 기반          myproject
                         명령어 입력 기반

왜 명령어를 쓰는가?
→ 원격 서버에는 화면(모니터)이 없음 → GUI 불가
→ 명령어는 자동화 가능 → 스크립트로 반복 실행
→ 동일한 작업을 수백 대 서버에 동시에 적용 가능
```

### 필수 명령어 완전 해설

```bash
# 탐색
ls          # List = 현재 폴더 내용 보기
ls -la      # -l(상세보기) -a(숨김파일 포함)
cd /경로    # Change Directory = 폴더 이동
cd ..       # 상위 폴더로 이동
pwd         # Print Working Directory = 현재 위치 출력

# 파일/폴더 관리
mkdir 폴더명        # Make Directory = 폴더 생성
mkdir -p a/b/c     # -p: 중간 경로도 한번에 생성
touch 파일명        # 빈 파일 생성
cp 원본 대상        # Copy = 복사
mv 원본 대상        # Move = 이동 (이름 변경에도 사용)
rm 파일명          # Remove = 삭제 (복구 불가!)
rm -rf 폴더명      # -r(폴더째로) -f(확인 없이) 삭제 주의!

# 파일 내용
cat 파일명          # 파일 전체 내용 출력
head -n 5 파일명   # 파일 앞 5줄 출력
tail -n 5 파일명   # 파일 뒤 5줄 출력
tail -f 파일명     # 파일 실시간 모니터링 (로그 볼 때 유용)
grep "단어" 파일명  # 파일에서 특정 단어 포함 줄 찾기

# 텍스트 작성
echo "내용"              # 화면에 텍스트 출력
echo "내용" > 파일명     # 파일에 쓰기 (덮어쓰기)
echo "내용" >> 파일명    # 파일에 추가 (기존 내용 유지)
nano 파일명              # 간단한 텍스트 편집기 열기

# 시스템
whoami                  # 현재 로그인된 계정 이름
sudo 명령어              # 관리자 권한으로 실행
su - 계정명             # 다른 계정으로 전환
exit                    # 현재 계정/세션 종료
```

---

## 01-3. 파일 권한 완전 이해

### 왜 권한이 필요한가?

```
[시나리오: 권한 없는 서버]

서버에 3명이 접속 중:
- 운영자 Alice   → 모든 파일 접근 필요
- 개발자 Bob     → 코드와 로그 파일만 필요
- 인턴 Carol     → 테스트 파일만 필요

권한이 없으면:
→ Carol이 실수로 API 키 파일 열어봄
→ Carol이 실수로 운영 데이터 삭제
→ 해커가 Bob 계정 탈취 시 모든 파일 접근 가능

권한이 있으면:
→ Carol은 자기 역할의 파일만 접근 가능
→ 실수 또는 해킹 시 피해 범위 최소화
→ 이것이 "최소 권한 원칙 (Principle of Least Privilege)"
```

### 권한 읽는 방법

```
ls -la 명령어 결과 예시:
-rwxr-x--- 1 agent-dev agent-core 1024 May 14 monitor.sh

분석:
위치  의미
①    - = 일반 파일 (d=폴더, l=링크)
②③④  rwx = 소유자(agent-dev)의 권한: 읽기+쓰기+실행
⑤⑥⑦  r-x = 그룹(agent-core)의 권한: 읽기+실행
⑧⑨⑩  --- = 나머지의 권한: 없음
     1    = 링크 수
     agent-dev  = 소유자
     agent-core = 그룹
     1024 = 파일 크기(바이트)
     monitor.sh = 파일명
```

### 숫자 권한 계산법

```
권한 종류:
r (읽기)  = 4
w (쓰기)  = 2
x (실행)  = 1
- (없음)  = 0

계산 방법: 가진 권한의 숫자를 더함

예시:
rwx = 4+2+1 = 7 (모든 권한)
r-x = 4+0+1 = 5 (읽기 + 실행)
r-- = 4+0+0 = 4 (읽기만)
--- = 0+0+0 = 0 (없음)

chmod 750 monitor.sh 해석:
→ 7: 소유자(rwx) = 읽기+쓰기+실행
→ 5: 그룹(r-x)  = 읽기+실행만
→ 0: 나머지(---) = 아무것도 못함

자주 쓰이는 권한:
755 = rwxr-xr-x = 실행 파일 (모두 실행 가능)
644 = rw-r--r-- = 일반 파일 (소유자만 수정)
600 = rw------- = 개인 키 파일 (소유자만 접근)
750 = rwxr-x--- = 그룹만 실행 (monitor.sh)
770 = rwxrwx--- = 소유자+그룹 모두 편집
```

### chmod와 chown 명령어

```bash
# chmod = Change Mode = 권한 변경
chmod 750 monitor.sh          # 숫자 방식
chmod u=rwx,g=rx,o= monitor.sh  # 문자 방식 (동일한 결과)

# chown = Change Owner = 소유자/그룹 변경
chown agent-dev monitor.sh              # 소유자만 변경
chown agent-dev:agent-core monitor.sh   # 소유자:그룹 동시 변경
chown -R agent-admin:agent-common /폴더 # -R: 하위 전체 적용
```

### ACL이란? (기본 권한의 한계 극복)

```
기본 권한의 한계:
→ 소유자 1명 + 그룹 1개 + 나머지 = 딱 3가지 구분만 가능

이번 과제 요구사항:
→ upload_files: agent-common 그룹 RW (admin+dev+test)
→ api_keys: agent-core 그룹만 RW (admin+dev만)
→ 두 디렉토리를 각각 다른 그룹으로 제어 → 기본 권한으로 가능

더 복잡한 상황 (ACL이 필요한 경우):
→ upload_files에서 agent-common은 RW, 특정 사용자만 X 추가
→ 이런 세밀한 설정은 기본 권한으로 불가 → ACL 사용

# setfacl = Set File ACL = ACL 규칙 추가
# -m = modify = 수정
# g:그룹명:권한 = 그룹에 권한 부여
# d: = default = 새로 만든 파일에도 적용

setfacl -m g:agent-common:rwx /경로
setfacl -m d:g:agent-common:rwx /경로

# getfacl = ACL 내용 확인
getfacl /경로
```

---

## 01-4. 계정과 그룹

```bash
# 계정 관련 명령어
useradd -m -s /bin/bash 계정명   # 계정 생성
passwd 계정명                    # 비밀번호 설정
su - 계정명                      # 계정 전환
id 계정명                        # 계정 정보 확인
whoami                          # 현재 계정 확인

# 그룹 관련
groupadd 그룹명                  # 그룹 생성
usermod -aG 그룹명 계정명         # 계정을 그룹에 추가
# -a = append (기존 그룹 유지) ← 반드시 필요!
# -G = groups

# 확인
cat /etc/passwd   # 전체 계정 목록
cat /etc/group    # 전체 그룹 목록
id 계정명          # uid/gid/groups 확인
```

---

## 01-5. 네트워크 기초 (SSH + 포트 + 방화벽)

### 포트(Port)란?

```
서버 = 아파트 건물 (IP = 주소)
포트 = 각 호수 (1호~65535호)

외부 접속:
"123.456.789.0:22" = "123번지 건물 22호에 연결해줘"

이번 과제:
22번 → 차단 (해커 자동 공격 방지)
20022번 → SSH 창구
15034번 → 앱 창구
나머지 → 모두 차단
```

### SSH란?

```
SSH = Secure Shell = 암호화된 원격 접속

내 컴퓨터 → 인터넷 → 서버 20022번 포트 → 인증 → 터미널 사용

왜 포트를 22에서 20022로 바꾸는가?
→ 해커 봇이 전 세계 모든 서버의 22번에 자동 공격
→ 20022로 바꾸면 봇이 감지 못함 → 공격 효과 없음

왜 Root 접속을 차단하는가?
→ Root = 모든 권한의 최고 관리자
→ Root로 해킹당하면 서버 완전 장악
→ 일반 계정으로 접속 → 필요 시 sudo만 사용
```

### UFW 방화벽

```bash
# UFW = Uncomplicated Firewall = 간단한 방화벽

# 기본 정책: 들어오는 것 모두 차단, 나가는 것 모두 허용
# 특정 포트만 명시적으로 허용

ufw allow 20022/tcp   # SSH 포트 허용
ufw allow 15034/tcp   # 앱 포트 허용
ufw enable            # 방화벽 활성화
ufw status            # 현재 상태 확인
ufw status verbose    # 상세 상태 확인
```

---

## 01-6. 환경 변수

```
환경 변수 = 프로그램이 참조하는 전역 설정값

비유: 스마트폰 "기본 앱" 설정
→ 링크 클릭 시 어떤 브라우저를 열지 = 환경 변수
→ 기본 브라우저를 Chrome으로 설정 = export BROWSER=chrome

이번 과제:
AGENT_HOME    = 앱의 기본 경로
AGENT_PORT    = 앱이 사용할 포트
AGENT_KEY_PATH = API 키 파일 경로
등...

코드에서 경로를 하드코딩하면:
log_path = "/home/agent-admin/agent-app/logs"
→ 경로 바뀌면 코드 수정 필요

환경 변수 사용하면:
log_path = os.environ.get("AGENT_LOG_DIR")
→ 환경 변수만 바꾸면 코드 수정 불필요
```

```bash
# 설정
export 변수명=값
export AGENT_HOME=/home/agent-admin/agent-app

# 사용
echo $AGENT_HOME           # 값 확인
ls $AGENT_HOME             # 경로로 사용
cat $AGENT_HOME/key.txt    # 경로 조합

# 영구 등록 (~/.bashrc에 추가)
echo 'export AGENT_HOME=/home/agent-admin/agent-app' >> ~/.bashrc
source ~/.bashrc            # 즉시 적용

# 전체 환경 변수 확인
env | grep AGENT
```

---

## 01-7. Shell Script 완전 기초

### 기본 구조

```bash
#!/bin/bash
# ↑ 첫 줄 필수: /bin/bash로 실행하라는 지시

# 주석 (실행 안 됨)
# 변수 (= 앞뒤 공백 없음!)
NAME="agent"         # 올바름
NAME = "agent"       # 에러! (공백 때문에)

# 변수 사용
echo $NAME           # agent
echo "이름: $NAME"   # 이름: agent
echo "${NAME}123"    # agent123 (이름 경계 명확히)

# 명령어 실행 결과를 변수에 저장
PID=$(pgrep -f "agent_app")   # $() 안의 명령 실행 후 결과 저장
DATE=$(date "+%Y-%m-%d")       # 오늘 날짜
```

### 조건문

```bash
# 기본 형태
if [ 조건 ]; then
    실행
fi

# 조건 종류
[ -z "$변수" ]     # 비어있으면 참 (zero length)
[ -n "$변수" ]     # 비어있지 않으면 참 (not empty)
[ -f "/경로" ]     # 파일이 존재하면 참
[ -d "/경로" ]     # 폴더가 존재하면 참
[ "$A" = "$B" ]    # 문자열 같으면 참
[ "$A" != "$B" ]   # 문자열 다르면 참
[ "$N" -gt 5 ]     # 숫자가 5보다 크면 참 (greater than)
[ "$N" -lt 5 ]     # 숫자가 5보다 작으면 참 (less than)
[ "$N" -eq 5 ]     # 숫자가 5와 같으면 참 (equal)

# 예시
if [ -z "$PID" ]; then
    echo "프로세스 없음"
    exit 1
fi
```

### 파이프와 리다이렉션

```bash
# 파이프 (|): 앞 명령의 출력 → 뒤 명령의 입력
ps aux | grep "agent_app"       # 모든 프로세스 중 agent_app 찾기
ss -tlnp | grep ":15034"        # 포트 목록 중 15034 찾기
free | grep "^Mem" | awk '{print $3/$2*100}'  # 여러 번 필터링

# 리다이렉션
echo "내용" > 파일   # 파일에 쓰기 (덮어쓰기)
echo "내용" >> 파일  # 파일에 추가
명령어 2>/dev/null   # 에러 메시지 숨기기
명령어 2>&1          # 에러를 일반 출력과 같은 곳으로
```

### 함수

```bash
# 함수 정의
함수명() {
    내용
    return 0   # 0=성공, 1=실패
}

# 함수 호출
함수명

# 예시
check_process() {
    PID=$(pgrep -f "agent-app")
    if [ -z "$PID" ]; then
        echo "[ERROR] 프로세스 없음"
        exit 1
    fi
    echo "[OK] PID: $PID"
}

check_process   # 함수 호출
```

---

## 01-8. crontab

```
crontab = 시간 기반 자동 실행 스케줄러

비유: 스마트폰 알람
→ "매일 오전 7시 알람" = "매분 monitor.sh 실행"
→ 한 번 등록하면 컴퓨터가 알아서 실행

문법:
분  시  일  월  요일  명령어
*   *   *   *   *   /경로/monitor.sh

* = 모든 값에 해당

예시:
* * * * *  /명령어       매분
0 * * * *  /명령어       매시 정각
0 9 * * *  /명령어       매일 오전 9시
0 9 * * 1  /명령어       매주 월요일 오전 9시
```

```bash
crontab -e    # 편집 (처음에는 편집기 선택 → nano)
crontab -l    # 목록 확인
crontab -r    # 전체 삭제 (주의!)

# [중요] cron은 환경 변수를 읽지 않음!
# → 스크립트 내에서 절대 경로 사용 필수
# → 또는 스크립트 내부에서 직접 변수 선언
```

---

## 01-9. 자주 나오는 시스템 명령어

```bash
# 프로세스 관련
ps aux              # 전체 프로세스 목록
pgrep -f "이름"     # 이름으로 PID 검색
kill PID            # 프로세스 종료

# 포트 관련
ss -tlnp            # TCP LISTEN 포트 목록
ss -tlnp | grep 15034  # 특정 포트 확인

# 시스템 자원
top                 # 실시간 CPU/메모리 사용률
top -bn1            # 한 번만 실행하고 종료 (스크립트용)
free                # 메모리 사용 현황
df /                # 루트 파티션 디스크 사용량
df -h               # 사람이 읽기 편한 형식 (GB, MB 단위)
stat -c%s 파일명    # 파일 크기 (바이트)

# 날짜/시간
date                # 현재 날짜/시간
date "+%Y-%m-%d %H:%M:%S"  # 포맷 지정 출력
# 결과: 2026-05-14 16:00:01

# 서비스 관리
service ssh start    # SSH 서비스 시작
service ssh restart  # SSH 서비스 재시작
service ssh status   # SSH 서비스 상태
```
